using UnityEngine;
using System.Collections;

public class titleAnimController : MonoBehaviour {

	// Use this for initialization
	void Start () {
        SoundManager.Instance.BGMTitleStart();
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
